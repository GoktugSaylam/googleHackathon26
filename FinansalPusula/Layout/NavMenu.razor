<div class="h-20 d-flex align-items-center px-4 mb-4">
    <div class="p-2 rounded-3 bg-primary bg-opacity-10 me-2">
        <i class="bi bi-compass text-primary fs-4"></i>
    </div>
    <span class="fs-5 fw-bold text-gradient">Finansal Pusula</span>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column gap-2">
        <div class="nav-item">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <i class="bi bi-house-door me-2"></i> Ana Sayfa
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="ekstre-yukle">
                <i class="bi bi-receipt-cutoff me-2"></i> Ekstre Yükle
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="butce-kalkani">
                <i class="bi bi-shield-lock me-2"></i> Bütçe Kalkanı
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="yatirim-takip">
                <i class="bi bi-graph-up-arrow me-2"></i> Yatırım Takip
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
