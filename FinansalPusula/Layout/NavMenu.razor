<div class="h-20 d-flex align-items-center px-4 mb-4">
    <div class="p-2 rounded-3 bg-primary bg-opacity-10 me-2">
        <i class="bi bi-compass text-primary fs-4"></i>
    </div>
    <span class="fs-5 fw-bold text-gradient">Finansal Pusula</span>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column gap-2">
        <div class="nav-item">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <i class="bi bi-house-door me-2 text-primary"></i> Ana Sayfa
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="ekstre-yukle">
                <i class="bi bi-card-checklist me-2 text-success"></i> Portföy Kataloğu
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="yatirim-takip">
                <i class="bi bi-search me-2 text-info"></i> Hisse Araştır
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="islemler">
                <i class="bi bi-journal-text me-2 text-warning"></i> İşlemler
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
